{
  "_comment": "Docs versions, newest first. The latest release has an empty path: it is served at the site root, and each new release takes that over. Shipping X.Y means setting latest, giving X.Y an empty path, and moving the version it replaces to vX.Y. Read by nix/docs.nix and the version dropdown, so the two cannot drift.",
  "latest": "0.3",
  "versions": [
    { "version": "main", "path": "main" },
    { "version": "0.3", "path": "" },
    { "version": "0.2", "path": "v0.2" }
  ]
}
